# North Health GPT — Repository Update (20 August 2026)

This update preserves the existing project documentation and adds targeted due-diligence improvements without replacing the working `northgpt/` prototype.

## Added

- two current prototype screenshots under `docs/screenshots/`;
- API security and secret-management documentation;
- model/provider strategy documentation;
- dependency-free repository validation;
- testing/evaluation documentation;
- explicit UNICEF open-source/funding alignment notes; and
- prototype-only safety labels for preliminary referral sample records.

## Deliberately not claimed

- Gemini Live has not been fine-tuned by North Health GPT;
- the dedicated Hausa ASR is not claimed as completed;
- dedicated Hausa TTS is not claimed as completed;
- the Android/offline platform is not claimed as completed;
- the 19-state facility database is not claimed as fully verified; and
- production clinical validation is not claimed from early usability testing.

## Security

`northgpt/api/config.php` is intentionally absent. The public `config.example.php` contains placeholders only.

## Prototype screenshots

The first screenshot in the README is the voice-first opening state supplied for the current evaluation demo. The second is the main Voice/Text interaction state.
